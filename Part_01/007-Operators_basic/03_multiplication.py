multiplicand = eval(input("Multiplicand:\t"))
multiplier = eval(input("Multiplier:\t"))

product = multiplicand * multiplier  # (*) Multiplication

print(f"{multiplicand}\tx\t{multiplier} =\t{product}")
