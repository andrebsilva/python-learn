dividend = eval(input("Dividend:\t"))
divisor = eval(input("Divisor:\t"))

integer_quotient = dividend // divisor      # (//) Integer of Division

print(f"{dividend}\t÷\t{divisor} =\t{integer_quotient}")
