"""
    VARIABLE:   |name|value|

    - It must begin with a letter: _azAZ (underscore is a letter)  

    - Case sensitive: upper or lower case are treated different

    - It uses assignment operator (=)

    - Special characters are NOT allowed

        ^ ~ : ; , . ` ' " - + / % = \ [ ] { } ( ) @ & ° | < > ! ? # $
        
    - For some reason, the ª and º are allowed, but NOT recommended

    - Reserved keywords are NOT allowed:

        False
        None
        True
        and
        as
        assert
        break
        class
        continue
        def
        del
        elif
        else
        except
        finally
        for
        from
        global
        if
        import
        in
        is
        lambda,nonlocal
        not
        or
        pass
        raise
        return
        try
        while
        with
        yield
"""